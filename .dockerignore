# --- VCS
.git
.gitignore
.gitattributes

# --- локальные env-файлы (но оставим примеры)
.env
.env.local
.env.*.local
!.env.example
!.env.sample
!.env.template

# --- логи
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# --- JS/Frontend зависимости
node_modules/
bower_components/
.pnp.cjs
.pnp.loader.mjs
.pnp.data.json

# --- сборка и кэш фронта
dist/
build/
.tmp/
.cache/
.parcel-cache/
.next/
.nuxt/
vite.cache/
coverage/
coverage.xml
*.tgz

# --- Python артефакты и кэш
__pycache__/
*.py[cod]
*.so
.mypy_cache/
.pytest_cache/
.ruff_cache/
.tox/
.venv/
venv/
env/
pip-wheel-metadata/
*.egg-info/
.eggs/

# --- Django артефакты/данные
# Статические файлы, собранные collectstatic (исходные 'static/' НЕ игнорируй).
staticfiles/
static_root/
# Загруженные пользователями файлы
media/
# dev-база, если вдруг используешь sqlite
*.sqlite3
db.sqlite3
# celery beat state
celerybeat-schedule

# --- Документация/офис
docs/_build/
*.ipynb
.ipynb_checkpoints/

# --- IDE/редакторы
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# --- ОС
.DS_Store
Thumbs.db

# --- Docker/Compose
# Dockerfile можно игнорить — он не отправляется в контекст, но не обязательно
Dockerfile*
docker-compose*.yml

# --- Разное/инфра
# Локальные данные сервисов если вдруг появились рядом
pg_data/
dump.rdb
